# Game-Sales-Analysis
Built an end-to-end ML pipeline predicting video game sales from 19,600 records. Cleaned data, handled missing values via median imputation, did EDA, encoded categorical features, then trained Linear Regression &amp; Random Forest models, evaluated using MAE/RMSE/R2
